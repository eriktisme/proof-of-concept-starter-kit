{
  "name": "aws-pipeline",
  "version": "0.1.0",
  "bin": {
    "app": "bin/app.js"
  },
  "scripts": {
    "build": "tsc",
    "cdk": "cdk",
    "lint": "eslint '**/*.ts'",
    "test": "jest --passWithNoTests",
    "typeCheck": "tsc --noEmit --emitDeclarationOnly false",
    "watch": "tsc -w"
  },
  "devDependencies": {
    "@eriktisme/aws-cdk-github-action-integration": "^0.0.0",
    "@internal/tsconfig": "workspace:*"
  }
}
